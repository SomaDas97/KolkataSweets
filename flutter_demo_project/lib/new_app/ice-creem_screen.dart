import 'package:flutter/cupertino.dart';

class IceCreemScreen extends StatelessWidget {
  const IceCreemScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ice-Cream"),
    );
  }
}
